// I have socket.io and socket.io-client installed. Not sure what to do beyond that. I do a GET request on the client to start the socket server.
import { NextApiRequest, NextApiResponse } from 'next';

export default function handler() {}

// export {serverSocket} // I want to export the server socket when it's created, so can call it in other files.
